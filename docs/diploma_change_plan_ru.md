# Правки к диплому с учетом новых добавлений

Проверенный файл диплома: `/Users/cukw/Downloads/Диплом_Кан (3).docx`.

Проверенные части проекта: `Backend/gateway/src/Controllers/AgentController.cs`, `Backend/services/AgentManagementService`, `LocalEndpointAgent/src/endpoint_agent`, `docker-compose.yml`, материалы `LocalEndpointAgent/README.md` и `docs/security/production_readiness.md`.

Главный вывод: диплом не нужно переписывать полностью. Основная структура уже подходит, но нужно согласовать основную часть с фактической реализацией LocalEndpointAgent, расширенной control plane, rollout/self-update, политиками, командами, аудитом и production-настройками.

## 1. Что удалить или заменить обязательно

### 1.1. Раздел 2.3, абзац про endpoint-агент

Место: раздел `2.3 Реализация функционала приложения`, после подписи `Таблица 13. Реализованные программные модули`.

Удалить или заменить текущий абзац, который начинается так:

```text
Важную роль играет endpoint-агент. В актуальной ветке основным рабочим агентом является ActivityAgent на C#, который отправляет события активности в ActivityService по gRPC. В отдельной ветке репозитория присутствует направление LocalEndpointAgent...
```

Вставить вместо него:

```text
Важную роль в актуальной реализации играет локальный агент LocalEndpointAgent. Он вынесен в отдельный модуль проекта `LocalEndpointAgent` и предназначен для установки на рабочие станции пользователей. Агент выполняет первичную регистрацию компьютера, связывает локальную конфигурацию с учетной записью пользователя, собирает разрешенные технические события активности, сохраняет их в локальной SQLite-очереди при отсутствии связи и передает накопленные события в ActivityService по gRPC. Для централизованного управления агент напрямую взаимодействует с AgentManagementService: отправляет heartbeat, получает политики, применяет версии политик и выполняет команды control plane.
```

Зачем: старая формулировка противоречит текущему коду. `ActivityAgent` в `Backend/services/ActivityAgent` нужно описывать как стендовый генератор активности, а не как основной endpoint-агент.

### 1.2. Таблица 13 `Реализованные программные модули`

Место: раздел `2.3 Реализация функционала приложения`, таблица `Таблица 13. Реализованные программные модули`.

Заменить строку:

```text
ActivityAgent | Backend/services/ActivityAgent | отправка событий активности в серверный контур
```

на строку:

```text
LocalEndpointAgent | LocalEndpointAgent/src/endpoint_agent | локальный кроссплатформенный агент: enrollment, сбор активности, локальная очередь, heartbeat, политики, команды, блокировка рабочей станции и self-update
```

Если нужно сохранить упоминание `ActivityAgent`, добавить отдельную строку ниже:

```text
ActivityAgent | Backend/services/ActivityAgent | стендовый генератор активности для демонстрации и smoke-проверок серверного контура
```

Зачем: сейчас таблица закрепляет неверный статус `ActivityAgent`.

### 1.3. Приложение 3, таблица сервисов docker-compose

Место: `Приложение 3. Схема развертывания и инфраструктуры`, таблица `Таблица 3.1. Сервисы и порты инфраструктурного контура`.

Удалить дублирующиеся строки в конце таблицы:

```text
backend | без публикации наружу | Сервис инфраструктурного или прикладного уровня
frontend | без публикации наружу | Web-клиент и обратный прокси для пользовательского доступа
```

Оставить строку:

```text
frontend | 3000:80, 3443:443 | Web-клиент и обратный прокси для пользовательского доступа
```

После таблицы добавить пояснение:

```text
LocalEndpointAgent не включается в серверный docker-compose, поскольку устанавливается непосредственно на рабочие станции пользователей. Серверный контур предоставляет для него gRPC-точки ActivityService и AgentManagementService, а также REST-маршруты Gateway для первичного входа, enrollment и завершения пользовательской сессии.
```

### 1.4. Раздел 2.4, одиночная строка `Разработка руководства`

Место: раздел `2.4 Тестирование приложения`, после абзаца о направлениях дальнейшего развития и перед строкой `Вывод по практической части`.

Текущую строку:

```text
Разработка руководства
```

лучше удалить или оформить как полноценный подраздел. Рекомендуемый вариант: заменить на абзац:

```text
На основании результатов тестирования подготовлены материалы для руководства администратора и пользователя. В руководство следует включить порядок входа в web-панель, просмотр Dashboard и Analytics, управление пользователями и компьютерами, работу с разделом Agents, изменение политик мониторинга, отправку команд агенту, запуск отчетов, просмотр уведомлений и базовые действия при установке LocalEndpointAgent на рабочую станцию.
```

### 1.5. Ошибки формулировок и дат

Место: глава 1, абзац после рисунка `1.13 Диаграмма последовательности для обработки событий`.

Заменить:

```text
Диаграмма прецедентов показана на рисунке 1.13.
```

на:

```text
Диаграмма последовательности показана на рисунке 1.13.
```

Место: Приложение 1, пункт `5.3. Сроки исполнения`.

Заменить:

```text
Начало работ: 02 февраля 2025 г.
```

на:

```text
Начало работ: 02 февраля 2026 г.
```

Место: Приложение 1, пункт `2.3. Требуемые показатели`.

Заменить пункт:

```text
- выявление аномальной активности и нарушений заданных политик.
```

на обычный пункт списка без лишнего дефиса:

```text
выявление аномальной активности и нарушений заданных политик.
```

## 2. Что добавить в основную часть

### 2.1. Введение

Место: `Введение`, после списка задач дипломной работы, после пункта:

```text
описать реализацию backend-сервисов, frontend-приложения, асинхронного обмена, баз данных и инфраструктуры развертывания;
```

Добавить пункт:

```text
разработать и описать локальный кроссплатформенный endpoint-агент, обеспечивающий enrollment рабочей станции, сбор разрешенной технической активности, локальную буферизацию событий, получение политик и выполнение команд control plane;
```

Если список становится слишком длинным, можно объединить этот пункт с уже существующим пунктом про реализацию backend/frontend, но LocalEndpointAgent нужно назвать явно.

### 2.2. Глава 1, раздел `Определение требований к информационной системе`

Место: после абзаца, который начинается с `Функциональные требования описывают конкретные функции программного продукта...`, перед таблицей функциональных требований или сразу после нее.

Добавить текст:

```text
С учетом развития локального агента требования к системе дополняются отдельным контуром работы endpoint-агента. Агент должен уметь выполнять первичный вход пользователя и enrollment рабочей станции через Gateway, хранить только служебные идентификаторы и refresh token, не сохранять пароль пользователя, собирать разрешенные технические признаки активности и устойчиво доставлять события в серверный контур. При недоступности серверов события должны помещаться в локальную SQLite-очередь и отправляться после восстановления связи.

Отдельным требованием является централизованная control plane. Администратор должен иметь возможность изменять политики сбора, включать или отключать отдельные коллекторы, управлять порогом риска, отправлять команды агенту, блокировать или разблокировать рабочую станцию, восстанавливать предыдущую версию политики и выполнять staged/canary rollout версии агента с контролем результата.
```

В таблицу функциональных требований добавить строки:

```text
Сбор расширенной локальной телеметрии | процессы, активное окно, idle time, история браузеров, сетевые соединения, изменения файлов, USB, inventory, сессии | LocalEndpointAgent с наборами collectors и локальным state
Устойчивая доставка событий | сохранение событий при потере связи и повторная отправка | SQLite-очередь агента, batch_id, event_id, sequence и flush-loop
Control plane агента | политики, версии политик, команды, retry, deadletter, блокировка рабочей станции | AgentManagementService, AgentController, LocalEndpointAgent command loop
Обновление агента | staged/canary rollout, desired_version, SELF_UPDATE, auto-rollback | endpoints `/api/agent/rollouts/plan`, `/api/agent/rollouts/execute` и команда `SELF_UPDATE`
Защита агентского канала | транспортный токен и проверка целостности команд/политик | gRPC metadata token `x-agent-token`, HMAC-SHA256 подпись control-plane payload
```

### 2.3. Глава 1, раздел `Проектирование информационной системы`

Место: после абзаца под рисунком `1.10. Диаграмма компонентов системы`, где описывается API Gateway и доменные сервисы.

Добавить текст:

```text
LocalEndpointAgent относится к клиентскому контуру системы и не разворачивается внутри серверного docker-compose. Он устанавливается на рабочие станции пользователей и взаимодействует с двумя серверными сервисами напрямую по gRPC: с ActivityService для отправки событий активности и с AgentManagementService для heartbeat, политик и команд. Первичная привязка агента к пользователю и компьютеру выполняется через Gateway: пользователь проходит login, после чего агент вызывает enrollment рабочей станции и сохраняет в локальной конфигурации computer_id, user_id, session_id, срок действия сессии и refresh token.
```

Место: после описания диаграммы последовательности обработки событий, перед рисунком `1.13` или сразу после него.

Добавить текст:

```text
Для локального агента последовательность обработки дополняется этапом буферизации. Если ActivityService недоступен, событие не теряется, а сохраняется в локальной SQLite-очереди. После восстановления связи flush-loop отправляет накопленные записи пакетами, помечает успешно отправленные события и сохраняет сведения об ошибках для последующего heartbeat. Это обеспечивает устойчивость сбора активности при кратковременных сетевых сбоях.
```

### 2.4. Раздел `2.1 Проектирование физической модели базы данных`

Место: после таблицы `Таблица 21. Состав баз данных проекта` или после первого абзаца раздела 2.1.

Добавить текст:

```text
Физическая модель AgentManagementService расширена таблицами и полями, необходимыми для полноценной control plane локальных агентов. Помимо карточки агента и sync-batches, база `agents` хранит политики, историю версий политик, команды, сведения о доставке команд и dead-letter записи. В карточку агента добавлены runtime-поля heartbeat: размер локальной очереди, время последнего сбора и отправки событий, последняя ошибка, версия примененной политики, capabilities, статусы коллекторов и исходная платформа.
```

Добавить в описание таблиц или отдельной таблицей:

```text
agents | хранит идентификатор компьютера, версию, статус, heartbeat, desired_version, health_json, queue_size, last_collected_at, last_sent_at, last_error, policy_version, capabilities_json, collector_statuses_json, source_platform
agent_policies | хранит интервалы сбора, heartbeat и flush, включение коллекторов, browser list, whitelist/blacklist, порог риска, auto_lock_enabled, admin_blocked и blocked_reason
agent_policy_versions | хранит снимки политик, тип изменения, автора изменения и время создания для последующего восстановления версии
agent_commands | хранит команды агентам, idempotency key, тип, payload, статус, количество попыток, timeout, next_retry_at, dead_letter_reason и результат выполнения
agent_command_dlq | хранит команды, переведенные в deadletter после исчерпания попыток доставки или timeout
```

Если в главе уже есть таблицы сущностей, не создавать дубли. Лучше добавить недостающие строки в существующие таблицы `Endpoint-агенты`, `Политики мониторинга`, `Команды агентов` и добавить отдельные сущности `Версии политик агентов` и `Dead-letter команд агентов`.

### 2.5. Раздел `2.2 Проектирование интерфейса информационной системы`

Место: после абзаца про экран `Раздел управления endpoint-агентами`, перед подписью `Рисунок 2.4. Раздел управления endpoint-агентами` или сразу после нее.

Добавить текст:

```text
В актуальной реализации раздел Agents предназначен не только для просмотра списка endpoint-агентов. Он отображает техническое состояние агента, включая очередь событий, последнюю отправку, последнюю ошибку, capabilities и статусы коллекторов. В карточке агента доступны политики мониторинга, история версий политик и восстановление выбранной версии. Также раздел поддерживает историю команд, фильтрацию команд по статусу, типу и периоду, повтор failed/timeout/deadletter-команд, отправку пользовательской команды, block/unblock рабочей станции и bulk-изменение состояния нескольких агентов. Для обновления версий предусмотрены операции rollout plan и rollout execute, которые позволяют выполнить полный, staged или canary rollout.
```

### 2.6. Раздел `2.3 Реализация функционала приложения`

Место: после абзаца, где перечисляются `AuthService`, `UserService`, `AgentManagementService`, `NotificationService`, `MetricsService`, `ReportService`.

Добавить текст:

```text
В Gateway для управления агентами реализован AgentController. Он предоставляет REST-маршруты для получения списка агентов, просмотра карточки агента, регистрации, изменения статуса, удаления, работы с политикой, просмотра и восстановления версий политик, постановки команд, повтора команд, block/unblock рабочей станции, bulk-операций и staged/canary rollout версий. Для операций, влияющих на состояние агента, используется журнал административного аудита, а для повторных запросов создания команд применяется idempotency key из заголовка `X-Idempotency-Key`.
```

Место: сразу после нового абзаца про LocalEndpointAgent из пункта 1.1.

Добавить текст:

```text
Внутри LocalEndpointAgent работа организована как набор асинхронных циклов: collection-loop собирает события из включенных collectors, flush-loop отправляет очередь событий в ActivityService, heartbeat-loop передает техническое состояние в AgentManagementService, policy-loop обновляет локальную политику, command-loop получает и выполняет команды, а lock-enforcement-loop повторно применяет административную блокировку при активном флаге политики. Такой подход разделяет сбор данных, доставку, управление и локальное реагирование.

Локальные collectors агента фиксируют снимки процессов, изменение активного окна, состояние активности или простоя пользователя, посещения сайтов из локальных браузеров, сетевые соединения, изменения файлов в заданных каталогах, подключение и отключение USB-устройств, inventory установленных приложений и процессов, а также события пользовательской сессии. Агент не реализует keylogging и не сохраняет вводимые пароли или текст пользовательского ввода.

Control plane поддерживает команды `BLOCK_WORKSTATION`, `UNBLOCK_WORKSTATION`, `FORCE_SYNC`, `UPDATE_POLICY` или `REFRESH_POLICY`, `RESTART_AGENT` и `SELF_UPDATE`. Команды проходят через состояния pending, running, success, failed, ignored, timeout и deadletter. При timeout сервис планирует повторную доставку с задержкой, а после исчерпания попыток переносит команду в dead-letter контур.
```

Место: после абзаца про Docker Compose.

Добавить текст:

```text
Для промышленного запуска добавлены меры production-readiness: секреты передаются через переменные окружения без небезопасных значений по умолчанию, поддержана ротация JWT-ключей через активный key id и несколько слотов ключей, CORS ограничен на уровне Gateway, а административные действия фиксируются в журнале аудита и могут быть переданы в SIEM-ready лог. Для эксплуатации подготовлены скрипты генерации секретов, ротации JWT-ключа, backup/restore PostgreSQL, blue/green deployment серверной части и canary rollout локальных агентов.
```

### 2.7. Раздел `2.4 Тестирование приложения`

Место: после абзаца, который начинается с `В проекте предусмотрен smoke E2E-сценарий...`.

Добавить текст:

```text
С учетом появления полноценного LocalEndpointAgent тестирование следует дополнить сценариями агентского контура. Проверяются enrollment рабочей станции через Gateway, корректное сохранение локальной конфигурации, завершение пользовательской сессии, работа локальной очереди при недоступности сервера, отправка накопленных событий после восстановления связи, получение политики, восстановление предыдущей версии политики, выполнение команд block/unblock, force sync и refresh policy, а также обработка timeout/deadletter и повтор команды.
```

В таблицу основных сценариев тестирования добавить строки:

```text
Enrollment агента | вход пользователя, привязка компьютера и сохранение session_id | агент получает computer_id, user_id, session_id и готов к запуску
Локальная очередь | отключение ActivityService и повторное включение | события сохраняются локально и отправляются после восстановления связи
Heartbeat health snapshot | передача queue_size, capabilities, collector_statuses и last_error | данные видны в карточке агента через Gateway
Версии политик | изменение политики и восстановление предыдущей версии | AgentManagementService сохраняет snapshot и возвращает восстановленную политику
Команды control plane | block/unblock, force sync, refresh policy, restart, self-update | команда проходит статусы и получает ack от агента
Retry и deadletter | имитация timeout команды | команда повторяется по расписанию или переводится в DLQ после исчерпания попыток
Rollout агента | plan/execute staged или canary rollout | формируются этапы rollout, создаются SELF_UPDATE-команды, при превышении порога ошибок выполняется rollback
Защита агентского канала | запрос без agent token и запрос с корректным token | gRPC-запрос без токена отклоняется, корректный запрос принимается
```

### 2.8. Заключение

Место: `Заключение`, перед финальным обобщающим абзацем.

Добавить текст:

```text
Дополнительно в ходе реализации был развит локальный агентский контур. LocalEndpointAgent обеспечивает регистрацию рабочей станции, сбор технической активности, локальную буферизацию событий, heartbeat с расширенным health snapshot, получение политик, выполнение команд control plane и управляемое обновление. Серверная часть дополнена историей версий политик, retry/deadletter-механизмом команд, staged/canary rollout агентов, административным аудитом и production-настройками безопасности. Это повышает практическую завершенность системы, поскольку мониторинг активности связан не только с серверной аналитикой, но и с управляемой эксплуатацией агентов на рабочих станциях.
```

## 3. Что добавить в приложения

### 3.1. Приложение 2 `Листинг приложения`

Место: после текущего фрагмента `Фрагмент управляющего цикла endpoint-агента`.

Добавить краткие фрагменты или хотя бы подписи к ним:

```text
Фрагмент конфигурационной модели LocalEndpointAgent: ServicesConfig, RuntimeConfig, CollectorsConfig, SecurityConfig и AgentIdentityConfig.
Фрагмент enrollment: вход через `/api/auth/login`, вызов `/api/user/computers/enroll`, сохранение computer_id, user_id, session_id, session_expires_at и refresh token.
Фрагмент обработки команд: BLOCK_WORKSTATION, UNBLOCK_WORKSTATION, FORCE_SYNC, REFRESH_POLICY, RESTART_AGENT, SELF_UPDATE.
Фрагмент health snapshot heartbeat: queue_size, last_collected_at, last_sent_at, last_error, policy_version, capabilities, collector_statuses, source_platform.
```

### 3.2. Приложение 4 `Карта API и сервисных контрактов`

Место: после таблицы `Основные REST-маршруты gateway`.

Добавить пояснение:

```text
Маршруты `/api/agent/rollouts/plan` и `/api/agent/rollouts/execute` предназначены для подготовки и выполнения обновления версии локального агента. Планирование формирует этапы full, staged или canary rollout, а выполнение создает SELF_UPDATE-команды для выбранных агентов и может инициировать rollback при превышении допустимой доли ошибок.
```

Место: после фрагмента gRPC-контракта управления агентами.

Добавить пояснение:

```text
gRPC-контракт AgentManagementService используется двумя типами клиентов. Gateway вызывает административные методы для web-панели, а LocalEndpointAgent вызывает агентские методы для регистрации, heartbeat, получения политики, получения pending-команд и подтверждения результата команды. Агентские вызовы защищаются transport token в metadata-заголовке, а payload политик и команд может подписываться HMAC-SHA256, чтобы агент мог проверять целостность control-plane данных.
```

### 3.3. Приложение 7 `Материалы тестирования`

Место: после таблицы smoke-проверки.

Добавить отдельную таблицу:

```text
Сценарий LocalEndpointAgent | Проверяемый результат
Первичный запуск без session_id | агент открывает login/enrollment и не стартует без активной сессии
Enrollment | создана или найдена рабочая станция, локально сохранены служебные идентификаторы
Сбор событий | формируются PROCESS_SNAPSHOT, BROWSER_VISIT, ACTIVE_WINDOW_CHANGE, USER_IDLE/USER_ACTIVE, NETWORK_CONNECTION, FILE_CREATED/FILE_MODIFIED/FILE_DELETED, USB_* и inventory-события
Недоступность ActivityService | события остаются в локальной SQLite-очереди
Восстановление связи | накопленные события отправляются, queue_size уменьшается
Команда block/unblock | агент применяет или снимает блокировку и отправляет ack
Policy refresh | агент получает новую версию политики и сохраняет policy_cache
Self-update | агент скачивает artifact, проверяет sha256 и подтверждает результат
```

### 3.4. Новое приложение или подраздел `Эксплуатационная готовность`

Место: после приложения с инфраструктурой или после материалов тестирования.

Добавить краткую сводку:

```text
Для эксплуатации подготовлены production-настройки и операционные скрипты. В docker-compose исключены небезопасные значения секретов по умолчанию, а пароли баз данных, RabbitMQ, Grafana, JWT-ключи и agent token передаются через переменные окружения. Для JWT добавлены активный key id и слоты ключей v1/v2, что позволяет выполнять ротацию без немедленного отключения всех пользователей. Ingress ограничивает подозрительные запросы и частоту обращений, Gateway ведет административный аудит, а SIEM-ready поток логов может использоваться для внешнего контроля. Для сопровождения подготовлены скрипты backup/restore PostgreSQL, генерации production-секретов, ротации JWT, blue/green deployment серверной части и canary rollout локальных агентов.
```

## 4. Что не удалять

Не удалять разделы технического задания, где уже описаны LocalEndpointAgent, локальная SQLite-очередь, gRPC-взаимодействие, политики, команды, безопасность и документация. Эти блоки уже хорошо согласуются с текущей реализацией. Их нужно только поддержать в основной части диплома, чтобы введение, глава 1, глава 2, заключение и приложения говорили об одном и том же фактическом решении.

Не удалять упоминание `ActivityAgent` полностью, если он используется для стенда. Нужно только изменить его роль: не основной endpoint-агент, а вспомогательный сервис для генерации активности и проверки серверного контура.

## 5. Рекомендуемый порядок внесения правок

1. Сначала заменить абзац в разделе 2.3 про `ActivityAgent` и строку в таблице 13. Это главная фактическая ошибка.
2. Затем добавить в 2.3 блок про LocalEndpointAgent, его collectors, loops, очередь, политики и команды.
3. После этого обновить требования в главе 1, чтобы они заранее объясняли появление LocalEndpointAgent и control plane.
4. Потом обновить раздел 2.1 по базе данных: `agent_policy_versions`, `agent_command_dlq`, runtime-поля агента.
5. Затем расширить раздел 2.2 про экран Agents и раздел 2.4 про тестирование.
6. В конце исправить приложения: API map, docker-compose, материалы тестирования и production-readiness.
7. Последним проходом исправить мелкие ошибки: дату 2025 -> 2026, неверную подпись `Диаграмма прецедентов`, лишний дефис в списке, дубли в таблице docker-compose и одиночную строку `Разработка руководства`.

